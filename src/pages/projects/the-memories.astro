---
import Project from "../../layouts/Project.astro";
import CaseMedia from "../../components/CaseMedia.astro";

/* ===== Page Meta ===== */
const page = {
  title: "The Memories",
  meta: "Portfolio • Front-End Build",
  lead:
    "เว็บไซต์พอร์ตโฟลิโอที่เจ้าของออกแบบเอง และอ้างอิงแนวคิดการจัดเลย์เอาต์จาก THE.GALLERY — โฟกัสอ่านง่าย โหลดไว และแยกหมวดชัดเจน",
  cover: "/img/project/thememories/hero.jpg", // ใช้ภาพบาง เบา ถ้ายังไม่มีทิ้งไว้ได้
  timeframe: "2024–2025",
  role: ["Frontend Development", "Performance Tuning", "UI Assembly"],
  stack: ["Astro", "CSS Utility (Grid/Flex/Radius/Shadow)", "Vanilla JS (เล็กน้อย)"],
  links: [],
};

/* ===== External Links ===== */
const links = {
  live: "https://inky3.github.io/Memories-of-G",
  projectNote: "https://inky3.github.io/the.gallery/projects/the-memories",
};
---

<Project
  {...page}
  heroTitleColor="#ffffff"
  heroLeadColor="#ffffff"
  heroAlign="center"
  heroValign="center"
  heroOverlay={0.12}
  heroHeight={200}
>
  <!-- ===== Overview ===== -->
  <section class="case-block">
    <h2 class="case-head">ภาพรวมโครงการ (Overview)</h2>
    <p>
      <b>The Memories</b> คือเว็บไซต์พอร์ตโฟลิโอที่เจ้าของงานออกแบบเอง
      โดยยึดแนวคิด “อ่านไว + ช่องว่างสูง” และขอยืมไอเดียการจัดหมวดและคอมโพเนนต์บางส่วนจาก
      <b>THE.GALLERY</b> ของผม — ผมรับผิดชอบฝั่ง <b>Front-End</b> ทั้งหมด
      (เซ็ต Astro, ประกอบ UI, ปรับภาพ และจูนความเร็ว)
    </p>
    <ul class="bullets">
      <li>โครงสร้างหน้า: <b>Home</b> (ฮีโร่ + แนะนำ) → <b>Portfolio</b> (Grid 3→2→1) → <b>About</b></li>
      <li>เป้าหมาย: เน้นเลย์เอาต์สะอาด, การ์ดอ่านชัด, ภาพโหลดไว, และตอบสนองมือถือ</li>
      <li>แรงบันดาลใจ: ระบบส่วนหัว/ชิปหมวด/ระยะหายใจจาก THE.GALLERY (ปรับใช้ให้เข้ากับแบรนด์)</li>
    </ul>
  </section>

  <!-- ===== Information Architecture ===== -->
  <section class="case-block band-soft">
    <h2 class="case-head">สถาปัตยกรรมข้อมูล (IA) & โฟลว์</h2>
    <div class="two-col">
      <div>
        <h3 class="h3">ไหลการใช้งาน</h3>
        <ul class="bullets">
          <li>Landing → Hook ด้วยคำโปรย/ภาพ และแถบเลื่อนสู่คอนเทนต์เด่น</li>
          <li>Portfolio → Grid การ์ดแบบ “ภาพนำ + Title + หมวด” คลิกเพื่อดูรายละเอียด</li>
          <li>About → โครงเรื่องสั้น กระชับ จุดขาย/ทักษะที่ชัดเจน</li>
        </ul>
      </div>
      <div>
        <h3 class="h3">การตัดสินใจเชิง UX</h3>
        <ul class="bullets">
          <li>เน้น <b>Whitespace</b> สูงเพื่อลด Cognitive Load</li>
          <li>ขนาดตัวอักษร/คอนทราสต์ผ่านเกณฑ์ WCAG (อ่านได้ในหลายสภาพแสง)</li>
          <li>Interactive ขั้นต่ำ — รักษาความเร็วเป็นอันดับแรก</li>
        </ul>
      </div>
    </div>
  </section>

  <!-- ===== Visual System (Lightweight) ===== -->
  <section class="case-block">
    <h2 class="case-head">ระบบภาพ & ส่วนประกอบ (Visual System)</h2>
    <ul class="bullets">
      <li><b>Brand Motif:</b> ฟิล์มสีส้ม-ทอง (มาจากดีไซน์ของเจ้าของงาน)</li>
      <li><b>Type & Grid:</b> Headline หนา/ใหญ่, กริด 3→2→1, รัศมีมุม/เงาเบา</li>
      <li><b>ภาพ:</b> ลดขนาดและบีบอัด, Lazy-load เฉพาะส่วนที่จำเป็น</li>
    </ul>

    <!-- รูปตัวอย่างหนึ่งภาพ (ย่อขนาดและจัดกึ่งกลาง) -->
    <div class="media sm">
      <CaseMedia image="/img/project/thememories/index.jpg" caption="การ์ดผลงานแบบภาพนำ + Title + Tag" />
    </div>
  </section>

  <!-- ===== Performance & Accessibility ===== -->
  <section class="case-block band-soft">
    <h2 class="case-head">ประสิทธิภาพ & การเข้าถึง (Perf & A11y)</h2>
    <ul class="bullets">
      <li>Astro ลด JavaScript ฝั่งไคลเอนต์ — โหลดเร็วบนเครือข่ายมือถือ</li>
      <li>รูปภาพกำหนดขนาด/สัดส่วนชัดเจน ป้องกัน Cumulative Layout Shift</li>
      <li>โครงหลากอุปกรณ์: ปรับกริดอัตโนมัติ, ปุ่ม/โซนแตะ ≥ 44px</li>
    </ul>
  </section>

  <!-- ===== Lessons & Next ===== -->
  <section class="case-block">
    <h2 class="case-head">สิ่งที่ได้เรียนรู้ & ขั้นถัดไป</h2>
    <ul class="bullets">
      <li>การ “ยืมภาษา” จากเทมเพลตเดิม ช่วยเร็วขึ้น แต่ยังต้อง <b>ตีความใหม่</b> ให้เข้าบุคลิกเจ้าของงาน</li>
      <li>คอนเทนต์ที่ต่างขนาดกันมาก ต้องมี <b>การ์ดมาตรฐานเดียว</b> เพื่อให้กริดนิ่ง</li>
      <li>ถัดไป: เพิ่มหน้ารายละเอียดผลงาน (Case Detail) และสคริปต์แปลงรูปอัตโนมัติ</li>
    </ul>
  </section>

  <!-- ===== CTA ===== -->
  <section class="section">
    <div class="container-narrow">
      <div class="deliver">
        <a class="cta big" href={links.live} target="_blank" rel="noreferrer">🚀 เปิดดูเว็บไซต์ The Memories</a>
      </div>
    </div>
  </section>
</Project>

<style is:global>
  /* ===== Blocks ===== */
  .case-block { padding: 3rem 0; display: grid; gap: 1rem; }
  .case-head  { font-size: 2rem; font-weight: 800; margin: 0 0 .6rem; }
  .band-soft  { background: var(--bg-soft,#f5f7f3); border-radius: 16px; padding: 3rem; }
  .bullets    { margin:.35rem 0 0; padding-left: 1.1rem; }
  .bullets li { margin:.25rem 0; }

  .two-col { display:grid; grid-template-columns:1fr 1fr; gap:1rem; }
  @media (max-width:900px){ .two-col{ grid-template-columns:1fr; } }

  /* Lightweight media figure */
  .media.sm :global(.frame),
  .media.sm :where(img,video){
    max-width: 720px; width: 100%; margin-inline:auto;
    border-radius: 14px; overflow: hidden;
  }
  @media (max-width:600px){
    .media.sm :global(.frame),
    .media.sm :where(img,video){ max-width: 540px; }
  }

  /* CTA buttons */
  .deliver{
    margin:1.1rem 0 0;
    display:flex; flex-wrap:wrap; gap:.65rem; justify-content:center;
  }
  .cta{
    display:inline-block; padding:.7rem 1.1rem; border-radius:999px;
    font-weight:800; text-decoration:none; transition:transform .15s ease, box-shadow .15s ease, background .15s ease;
    box-shadow:0 3px 10px rgba(0,0,0,.08);
  }
  .cta.big{   background:#111; color:#fff; border:1px solid rgba(0,0,0,.06); }
  .cta.big:hover{ transform:translateY(-2px); box-shadow:0 10px 20px rgba(0,0,0,.20); }
  .cta.ghost{ background:#fff; color:#111; border:1px solid rgba(0,0,0,.12); }
  .cta.ghost:hover{ transform:translateY(-2px); box-shadow:0 10px 18px rgba(0,0,0,.12); }
</style>
