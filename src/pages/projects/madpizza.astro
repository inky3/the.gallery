---
import Main from "../../layouts/Main.astro";

/** Meta */
const hero = {
  head: "Mad Pizza — Digital Menu (iPad)",
  description:
    "โปรเจกต์ออกแบบเมนูดิจิทัลบน iPad สำหรับร้าน MAD PIZZA ที่สร้างบน Figma เท่านั้น (ยังไม่พัฒนาเว็บจริง) โฟกัส UX/UI สำหรับการใช้งานหน้าร้าน: อ่านง่าย แตะง่าย โหลดเบา และสอดคล้องอัตลักษณ์สีแดง–ส้มของแบรนด์"
};

/** Path Helper (for GitHub Pages) */
const base = import.meta.env.BASE_URL || "/";
const url = (p = "/") => `${base}${String(p).replace(/^\/+/, "")}`;

/** Links */
const links = {
  prototype:
    "https://www.figma.com/proto/XArrul0wbneI0hyZP4mntO/Madpizza?page-id=0%3A1&node-id=1-5&p=f&viewport=210%2C143%2C0.17&t=1VNQwdh78BC66g6O-1&scaling=scale-down&content-scaling=fixed&starting-point-node-id=1%3A5",
};
---

<Main head={hero.head} description={hero.description}>
  <!-- Overview Section -->
  <section id="overview" class="container py-4 pb-1">
    <div class="row g-4 align-items-start">
      <div class="col-12 col-lg-7">
        <figure class="frame">
          <img
            src={url("/img/project/madpizza.jpg")}
            alt="หน้าเมนู MAD PIZZA"
            loading="lazy"
            decoding="async"
          />
        </figure>
      </div>

      <div class="col-12 col-lg-5">
        <h2 class="h4 mb-3">ภาพรวมโปรเจกต์</h2>
        <p class="lead-sm">
          โปรเจกต์นี้เป็นการออกแบบ <strong>เมนูดิจิทัลสำหรับ iPad</strong> 
          เพื่อใช้จริงในร้าน <em>MAD PIZZA</em> โดยมีคอนเซ็ปต์ “เมนูที่อบอุ่น สดใหม่ เหมือนพิซซ่าจากเตา”
          เน้นดีไซน์ที่สะอาด กดง่าย และเหมาะกับการใช้งานหน้าร้านจริง
        </p>

        <div class="meta mt-3">
          <div>
            <span class="k">บทบาท</span>
            <span class="v">
              ออกแบบ UX/UI และทำ Prototype บน Figma ครบทั้งเลย์เอาต์, ดีไซน์โทน, Micro-interaction, และสเปกส่งต่อทีมพัฒนา (Design Handoff)
            </span>
          </div>

          <div>
            <span class="k">เทคโนโลยี</span>
            <span class="v">
              Figma (Auto-Layout, Variants/Components, Design Tokens, Prototyping, Interaction Map)
            </span>
          </div>

          <div>
            <span class="k">ขอบเขต</span>
            <span class="v">
              โครงเมนูสำหรับ iPad: Hero/Brand, แถบหมวด, การ์ดเมนู, ราคา/ป้ายโปร, สถานะสั่งซื้อ, และแนวทาง Responsive สำหรับ Tablet แนวตั้ง/แนวนอน
            </span>
          </div>

          <div>
            <span class="k">สถานะ</span>
            <span class="v">Prototype (Figma)</span>
          </div>
        </div>

        <div class="mt-4 d-flex gap-2 flex-wrap">
          <a href={links.prototype} target="_blank" rel="noreferrer" class="btn-cta">
            ดู Figma Prototype
          </a>
          <a href={url("/projects")} class="btn-ghost">กลับไป Projects</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Menu Showcase -->
  <section id="menu" class="container py-5">
    <h3 class="h5 mb-3">ตัวอย่างการ์ดเมนู (Spec สำหรับ Dev – อ้างอิงจาก Figma)</h3>
    <div class="row g-4">
      <div class="col-12 col-md-4">
        <div class="menu-card">
          <img src={url("/img/project/madpizza/truffella.jpg")} alt="Truffella" />
          <div class="info">
            <div class="name">TRUFFELLA</div>
            <div class="price">340.-</div>
          </div>
        </div>
      </div>
      <div class="col-12 col-md-4">
        <div class="menu-card">
          <img src={url("/img/project/madpizza/lamb-bizkit.jpg")} alt="Lamb Bizkit" />
          <div class="info">
            <div class="name">LAMB BIZKIT</div>
            <div class="price">370.-</div>
          </div>
        </div>
      </div>
      <div class="col-12 col-md-4">
        <div class="menu-card">
          <img src={url("/img/project/madpizza/maddy-mighty-meaty.jpg")} alt="Maddy Mighty Meaty" />
          <div class="info">
            <div class="name">MADDY MIGHTY MEATY</div>
            <div class="price">395.-</div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="container py-4">
    <div class="cta">
      <a href={links.prototype} target="_blank" rel="noreferrer" class="btn-cta">เปิด Figma Prototype</a>
      <a href={url("/projects")} class="btn-ghost">ย้อนกลับ Projects</a>
    </div>
  </section>

  <style>
    /* Frame */
    .frame {
      border-radius: 18px;
      overflow: hidden;
      border: 1px solid rgba(0,0,0,.12);
      box-shadow: 0 10px 24px rgba(0,0,0,.18);
      background: #0e0e0e;
    }
    .frame img { display:block; width:100%; height:auto; }

    /* Meta grid */
    .meta { display:grid; row-gap:.45rem; font-size:.95rem; }
    .meta div { display:flex; align-items:flex-start; }
    .meta .k {
      display:inline-block; min-width:90px; color:#666; font-weight:600; line-height:1.6; flex-shrink:0;
    }
    .meta .v {
      flex:1; color:#111; line-height:1.6; text-align:justify; text-justify:inter-word; word-break:break-word;
    }

    /* Buttons */
    .btn-cta, .btn-ghost {
      display:inline-block; border-radius:999px; padding:.6rem 1.2rem; font-weight:600;
      text-decoration:none; transition:transform .15s ease, box-shadow .15s ease, background .2s ease, color .2s ease;
    }
    .btn-cta { background:#C02820; color:#fff; border:1px solid #C02820; box-shadow:0 4px 12px rgba(0,0,0,.15); }
    .btn-cta:hover { transform:translateY(-1px); }
    .btn-ghost { background:#fff; color:#222; border:1px solid rgba(0,0,0,.12); }
    .cta { display:flex; gap:.6rem; justify-content:center; }

    /* Menu cards */
    .menu-card {
      border-radius:16px;
      border:1px solid rgba(0,0,0,.1);
      background:#fff;
      overflow:hidden;
      box-shadow:0 6px 12px rgba(0,0,0,.08);
      transition:transform .2s ease, box-shadow .2s ease;
    }
    .menu-card:hover {
      transform:translateY(-3px);
      box-shadow:0 10px 20px rgba(0,0,0,.12);
    }
    .menu-card img {
      width:100%;
      height:220px;
      object-fit:cover;
      background:#f3f3f3;
    }
    .menu-card .info {
      padding:.8rem 1rem;
      display:flex;
      justify-content:space-between;
      align-items:center;
    }
    .menu-card .name {
      font-weight:700;
      color:#111;
    }
    .menu-card .price {
      font-weight:600;
      color:#C02820;
    }

    @media (max-width: 575.98px) {
      .menu-card img { height:180px; }
      .meta .k { min-width:75px; }
    }
  </style>
</Main>
