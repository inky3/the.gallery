---
import Project from "../../layouts/Project.astro";
import CaseMedia from "../../components/CaseMedia.astro";

/* BASE-URL safe helpers */
const base = import.meta.env.BASE_URL || "/";
const href = (p="/") => `${base}${String(p).replace(/^\/+/, "")}`;
const enc  = (p="/") => href(p).replace(/ /g, "%20");

const page = {
  title: "Good Health",
  meta: "UX/UI • Health",
  lead:
    "ออกแบบระบบ UX/UI เพื่อช่วยให้ผู้ใช้เลือกเมนูที่เหมาะกับการควบคุมน้ำตาล — โฟกัสการอ่านเร็ว, ข้อมูลสำคัญชัด, ลดภาระการตีความ",
  cover: "/img/project/goodhealth/nav.png",
  timeframe: "2024",
  role: ["UX Research", "Information Architecture", "UI Design"],
  stack: ["Figma", "Design System", "Responsive UI"],
};
---

<Project
  {...page}
  heroTitleColor="#ffffff"
  heroLeadColor="#ffffff"
  heroAlign="center"
  heroValign="center"
  heroOverlay={0.05}
  heroHeight={130}
>

  <!-- MOBILE DOCK (sticky on phones) -->
  <nav class="cta-dock" aria-label="Project Links">
    <a href="https://drive.google.com/file/d/1HnlALmfTK6ntb2a2rtp_ST3bsKMY7YdE/view" target="_blank" rel="noreferrer">Paper</a>
    <a href="https://drive.google.com/file/d/12PMAxFaPCV67GCpzpDs6FtL1luHiIYoP/view"   target="_blank" rel="noreferrer">Slides</a>
    <a href="https://www.figma.com/proto/LmXawaOlg1a0YjpFD2dgxO/Good-Health-PC?node-id=2132-1484&t=qbRhSrqPoewXmOmL-1"   target="_blank" rel="noreferrer">Figma D</a>
    <a href="https://www.figma.com/proto/fYKSdYY2OeLrp7tle23084/Untitled?node-id=58-912&t=Wf7hhptUifObVHl0-1"    target="_blank" rel="noreferrer">Figma M</a>
  </nav>

  <!-- OVERVIEW -->
  <section class="case-block">
    <h2 class="case-head">ภาพรวมโครงการ (Overview)</h2>
    <p>
      ผลงานนี้วางโครง UX และระบบหน้าการ์ดเมนูอาหารสำหรับผู้ควบคุมน้ำตาลและผู้ใช้งานทั่วไป
      แนวทางคือทำให้ “สิ่งที่ต้องรู้” ปรากฏก่อน—โดยเฉพาะตัวเลขสำคัญอย่าง
      <b>Carb / Sugar / kcal</b>—เพื่อลดเวลาการตัดสินใจและลดภาระการตีความจากข้อมูลยาว ๆ
    </p>
  </section>

  <!-- SCOPE -->
  <section class="case-block band-soft">
    <h2 class="case-head">ขอบเขตงาน (Design Scope)</h2>
    <ul>
      <li>Research → สัมภาษณ์/สังเกตพฤติกรรมผู้ใช้ + รวบรวมประเด็น Pain Points</li>
      <li>IA → โครงสร้างข้อมูลจาก “ภาพรวม → รายละเอียด → เปรียบเทียบ”</li>
      <li>UI Layout → การ์ดเมนูแบบ <i>scan-first</i> พร้อม Key Metrics เด่น</li>
      <li>Visual System → สี/ตัวอักษรอ่านง่าย ภายใต้คอนเซ็ปต์สุขภาพที่น่าเชื่อถือ</li>
      <li>Responsive → ออกแบบมือถือเป็นฐาน แล้วขยายสู่เดสก์ท็อป</li>
    </ul>
  </section>

  <!-- USERS & PROBLEMS -->
  <section class="case-block">
    <h2 class="case-head">ผู้ใช้และ Pain Points (Users & Problems)</h2>
    <div class="two-col">
      <div>
        <h3 class="h3">Target Users</h3>
        <ul>
          <li><b>ผู้เป็นเบาหวาน</b> ต้องเห็นค่าที่เสี่ยงทันที (Carb / Sugar)</li>
          <li><b>ผู้เริ่มดูแลสุขภาพ</b> ต้องการสแกนเร็ว ไม่ต้องอ่านยาว</li>
        </ul>
      </div>
      <div>
        <h3 class="h3">Pain Points</h3>
        <ul>
          <li>ข้อมูลกระจายหลายหน้า → ใช้แรงประมวลผลสูง</li>
          <li>เส้นทางค้นหาเมนูยาว → หลายคลิกกว่าจะได้คำตอบ</li>
          <li>ตัวเลขสำคัญไม่เด่น → เปรียบเทียบยาก</li>
        </ul>
      </div>
    </div>
    <p class="highlight">เห็นสิ่งสำคัญใน 1 สแกน → เปรียบเทียบง่าย → ตัดสินใจเร็วขึ้น</p>
  </section>

  <!-- DESIGN GOALS -->
  <section class="case-block band-soft">
    <h2 class="case-head">เป้าหมายการออกแบบ (Design Goals)</h2>
    <ol class="goals">
      <li>ลดภาระการคิด (Cognitive Load) ด้วยลำดับข้อมูลที่คาดเดาได้</li>
      <li>ยกตัวเลขสำคัญขึ้นก่อน (Key Metric First) เพื่อการสแกนเร็ว</li>
      <li>ค้นหาเร็วด้วยหมวดหมู่และชิป (Filter Chips)</li>
      <li>ถอดความหมายศัพท์/ตัวเลขให้เป็นภาษาคนทั่วไป</li>
    </ol>
  </section>

  <!-- IA & FLOW -->
  <section class="case-block">
    <h2 class="case-head">โครงสร้างข้อมูลและการนำทาง (IA & Flow)</h2>
    <p>ไหลจาก <b>ภาพรวม</b> → <b>รายละเอียด</b> → <b>เปรียบเทียบ</b> เพื่อลดจำนวนคลิกและทำให้ได้คำตอบไว</p>
    <div class="media-grid">
      <CaseMedia image="../../img/project/goodhealth/menu-plan.jpg"  caption="Flow เลือกมื้ออาหาร" />
      <CaseMedia image="../../img/project/goodhealth/fruit-info.jpg" caption="การ์ดเน้นตัวเลขสำคัญ (Key Metric Highlight)" />
    </div>
  </section>

  <!-- UI SYSTEM -->
  <section class="case-block band-soft">
    <h2 class="case-head">UI System (สรุป)</h2>
    <ul>
      <li><b>โทนสี:</b> เขียวอ่อน–ครีม → สงบ น่าเชื่อถือ</li>
      <li><b>Grid:</b> โปร่ง อ่านง่าย</li>
      <li><b>Card:</b> ตัวเลขสำคัญต้องชัด → อ่านแล้วเข้าใจเร็ว</li>
    </ul>
    <div class="media-grid">
      <div class="phone">
        <CaseMedia image="../../img/project/goodhealth/index-phone.jpg" caption="Mobile Layout" ratio="9/19.5" />
      </div>
      <div class="phone">
        <CaseMedia image="../../img/project/goodhealth/menu-plan-phone.jpg" caption="Quick Meal Plan" ratio="9/19.5" />
      </div>
      <div class="phone">
        <CaseMedia image="../../img/project/goodhealth/profile-phone.jpg" caption="User Profile" ratio="9/19.5" />
      </div>
      <CaseMedia image="../../img/project/goodhealth/index.jpg" caption="Landing Page" />
    </div>
  </section>

  <!-- NEW: Component Inventory (adds detail without removing anything) -->
  <section class="case-block">
    <h2 class="case-head">Component Inventory</h2>
    <ul>
      <li>Navigation chips (state: default / active / disabled)</li>
      <li>Card: metric header • image body • actions footer (save / compare)</li>
      <li>Filter sheet (range sliders for Carb/Sugar/kcal + quick presets)</li>
      <li>Empty state / Error state / Loading skeleton</li>
      <li>Toast: “Saved to Favorites”, “Added to Compare”</li>
    </ul>
  </section>

  <!-- ACCESSIBILITY & READABILITY -->
  <section class="case-block band-soft">
    <h2 class="case-head">การเข้าถึงและการอ่าน (Accessibility & Readability)</h2>
    <ul>
      <li>คอนทราสต์ตัวอักษรตามแนวทาง WCAG เป็นหลัก</li>
      <li>ขนาดตัวอักษรและช่องไฟที่อ่านสบายบนมือถือ</li>
      <li>ไอคอน/ชิปมีคำอธิบาย (aria-label) เพื่อผู้อ่านจอ screen reader</li>
      <li>ปุ่มกด/พื้นที่สัมผัส ≥ 44px เพื่อการแตะที่แม่นยำ</li>
    </ul>
  </section>

  <!-- NEW: Content Strategy -->
  <section class="case-block">
    <h2 class="case-head">Content Strategy</h2>
    <ul>
      <li>ใช้คำศัพท์สั้น จำง่าย (“Sugar”, “Carb”, “kcal”) พร้อม tooltip อธิบาย</li>
      <li>กำจัด jargon ทางโภชนาการที่ไม่จำเป็นในการตัดสินใจ</li>
      <li>สรุปข้อมูลเป็น bullet + icon เล็ก ๆ สำหรับสแกนเร็ว</li>
    </ul>
  </section>

  <!-- NEW: Evaluation & Metrics -->
  <section class="case-block band-soft">
    <h2 class="case-head">Evaluation & Metrics</h2>
    <ul>
      <li>Time-to-Answer (TTA): เวลาจากเปิดหน้า → เจอเมนูที่เหมาะสม (เป้าหมาย &lt; 15s)</li>
      <li>First-Fixation: สัดส่วนผู้ใช้ที่สายตาไปตกที่ Key Metrics ภายใน 1s แรก</li>
      <li>Click Depth: จำนวนคลิกเฉลี่ยต่อการค้นหาหนึ่งครั้ง (ลดลง ≥ 30%)</li>
      <li>Task Success Rate: ≥ 85% ในงาน “เลือกเมนูที่ปลอดภัย/เหมาะสม”</li>
    </ul>
  </section>

  <!-- NEW: Edge Cases -->
  <section class="case-block">
    <h2 class="case-head">Edge Cases</h2>
    <ul>
      <li>ไม่มีเมนูที่ตรงเงื่อนไข → แนะนำการผ่อน/สลับเงื่อนไขที่ยังปลอดภัย</li>
      <li>ข้อมูลโภชนาการไม่ครบ → แสดงสัญลักษณ์เตือน + ลิงก์ดูที่มา</li>
      <li>สัญญาณเน็ตช้า → ใช้ skeleton + cache การ์ดยอดนิยม</li>
    </ul>
  </section>

  <!-- LEARNINGS & NEXT -->
  <section class="case-block band-soft">
    <h2 class="case-head">สิ่งที่ได้เรียนรู้ & ขั้นถัดไป (Learnings & Next Steps)</h2>
    <ul>
      <li>ผู้ใช้ต้องการเห็น “คำตอบ” เร็วกว่า “คำอธิบาย” → การ์ดแบบ scan-first ทำงานได้ดี</li>
      <li>ถัดไป: เพิ่ม Quick-Compare ระหว่างการ์ด และโหมด “เมนูโปรด/บันทึกไว้ก่อน”</li>
      <li>เตรียมทดสอบ A/B กับตำแหน่ง Key Metrics เพื่อหาการจัดวางที่เร็วที่สุด</li>
    </ul>
  </section>

  <!-- NEW: Handoff -->
  <section class="case-block">
    <h2 class="case-head">Handoff</h2>
    <ul>
      <li>Design tokens (สี/ตัวอักษร/spacing) + component specs ใน Figma</li>
      <li>Interaction notes (hover/focus/active/disabled) แนบในแต่ละ component</li>
      <li>Prototype flows: Landing → Filter → Compare → Save Favorites</li>
    </ul>
  </section>

<!-- BOTTOM DELIVERABLES CTA BAR (only at bottom) -->
  <section class="deliverables">
    <div class="container-center cta-rail bottom">
      <a class="btn-pill solid" href="https://drive.google.com/file/d/1HnlALmfTK6ntb2a2rtp_ST3bsKMY7YdE/view" target="_blank" rel="noreferrer">Research Paper (PDF)</a>
      <a class="btn-pill solid" href="https://drive.google.com/file/d/12PMAxFaPCV67GCpzpDs6FtL1luHiIYoP/view"   target="_blank" rel="noreferrer">Presentation Slides (PDF)</a>
      <a class="btn-pill solid" href="https://www.figma.com/proto/LmXawaOlg1a0YjpFD2dgxO/Good-Health-PC?node-id=2132-1484&t=qbRhSrqPoewXmOmL-1" target="_blank" rel="noreferrer">Figma — Desktop</a>
      <a class="btn-pill solid" href="https://www.figma.com/proto/fYKSdYY2OeLrp7tle23084/Untitled?node-id=58-912&t=Wf7hhptUifObVHl0-1"  target="_blank" rel="noreferrer">Figma — Mobile</a>
    </div>
  </section>


</Project>

<style is:global>
  /* Base blocks */
  .case-block { padding:3rem 0; display:grid; gap:1rem; }
  .band-soft { background:var(--bg-soft,#f5f7f3); border-radius:16px; padding:3rem; }
  .case-head { font-size:2rem; font-weight:800; margin:0 0 .6rem; }
  .muted { color:var(--text-muted,#5c6670); margin:.25rem 0 1rem; }

  /* Links */
  .link-rail { display:flex; flex-wrap:wrap; gap:.6rem; }
  .btn-pill{
    display:inline-block; padding:.65rem 1.05rem; border-radius:999px;
    background:var(--accent,#0f6657); color:#fff; font-weight:700;
    text-decoration:none; white-space:nowrap;
  }

  /* Mobile link dock */
  .cta-dock{
    position: sticky; bottom:.75rem; z-index:20; margin-top:-1rem;
    display:none; gap:.5rem; justify-content:center;
  }
  .cta-dock a{
    background:#101418; color:#fff; border:1px solid rgba(255,255,255,.12);
    padding:.55rem .8rem; border-radius:10px; font-weight:700; text-decoration:none;
  }
  @media (max-width: 720px){
    .cta-dock{ display:flex; flex-wrap:wrap; }
  }

  /* Layout helpers */
  .pill-list { display:flex; flex-wrap:wrap; gap:.5rem; }
  .pill { padding:.45rem 1rem; border-radius:999px; background:#e9ecee; font-weight:600; }
  .two-col { display:grid; grid-template-columns:1fr 1fr; gap:1rem; }
  @media (max-width:900px){ .two-col{ grid-template-columns:1fr; } }
  .goals { margin:.25rem 0 0 1rem; }

  .highlight{
    background:#e8f6ef; padding:.8rem 1rem; border-left:4px solid var(--accent,#0f6657); border-radius:8px;
  }

  .media-grid { --cols:2; display:grid; gap:.9rem; grid-template-columns:repeat(var(--cols),minmax(0,1fr)); }
  @media (max-width:900px){ .media-grid{ --cols:1; } }

  /* Phone frames — REDUCED SIZE (as requested) */
  .phone { max-width:260px; margin-inline:auto; }
  @media (max-width:560px){ .phone{ max-width:220px; } }
  .phone .frame{ border-radius:22px; overflow:hidden; }

  /* Bottom Deliverables CTA bar */
  .deliverables{ padding:1.75rem 0 2.75rem; background:linear-gradient(180deg, rgba(0,0,0,0) 0%, var(--bg-page,#f7f8f9) 42%); }
  .container-center{ width:min(1100px,92vw); margin-inline:auto; }
  .cta-rail.bottom{ justify-content:center; display:flex; flex-wrap:wrap; gap:.6rem; }
  .btn-pill.solid{ background:var(--accent,#0f6657); color:#fff; }

  /* ===== CTA buttons (hover anim) ===== */
.btn-pill{
  display:inline-block;
  padding:.65rem 1.05rem;
  border-radius:999px;
  background:var(--accent,#0f6657);
  color:#fff;
  font-weight:700;
  text-decoration:none;
  white-space:nowrap;
  transform: translateY(0) scale(1);
  transition: transform .18s ease, box-shadow .18s ease, filter .18s ease;
  will-change: transform, box-shadow, filter;
}
.btn-pill:hover{
  transform: translateY(-2px) scale(1.015);
  box-shadow: 0 10px 20px rgba(0,0,0,.12);
  filter: brightness(1.03);
}
.btn-pill:active{
  transform: translateY(0) scale(.995);
  box-shadow: 0 6px 12px rgba(0,0,0,.10);
}
.btn-pill:focus-visible{
  outline: none;
  box-shadow: 0 0 0 3px rgba(15,102,87,.35), 0 10px 20px rgba(0,0,0,.12);
}

/* Respect reduced motion */
@media (prefers-reduced-motion: reduce){
  .btn-pill,
  .btn-pill:hover,
  .btn-pill:active{
    transition: none;
    transform: none !important;
  }
}

/* ===== Bottom Deliverables CTA bar ===== */
.deliverables{
  padding:1.75rem 0 2.75rem;
  background: linear-gradient(180deg, rgba(0,0,0,0) 0%, var(--bg-page,#f7f8f9) 42%);
}
.container-center{ width:min(1100px,92vw); margin-inline:auto; }
.cta-rail.bottom{
  justify-content:center;
  display:flex;
  flex-wrap:wrap;
  gap:.6rem;
}
.btn-pill.solid{ background:var(--accent,#0f6657); color:#fff; }

/* ===== Dark band (reusable hero/section) =====
   Use:
   <section class="band-dark band-center" style="--band-image:url('/img/hero.jpg'); --band-overlay:.45; --band-height:360px;">
     <h1>Title</h1><p>Lead</p>
   </section>
*/
.band-dark{
  --band-image: none;
  --band-overlay: .50;   /* 0–1 */
  --band-height: 320px;  /* you can override per section */
  position: relative;
  min-height: var(--band-height);
  padding: clamp(32px, 6vw, 72px) 0;
  display: grid;
  align-items: center;
  color: #fff;
  overflow: hidden;
  isolation: isolate; /* keep overlay scoped */
}
.band-dark::before{
  content:"";
  position:absolute; inset:0;
  background:
    linear-gradient(180deg, rgba(0,0,0,.0) 0%, rgba(0,0,0,.08) 25%, rgba(0,0,0,.18) 60%, rgba(0,0,0,.24) 100%),
    rgba(0,0,0,var(--band-overlay));
  z-index:0;
}
.band-dark::after{
  content:"";
  position:absolute; inset:0;
  background-image: var(--band-image);
  background-size: cover;
  background-position: center;
  background-repeat: no-repeat;
  z-index:-1;
  filter: saturate(.9) contrast(.95);
}
.band-dark > *{ position: relative; z-index: 1; text-shadow: 0 2px 18px rgba(0,0,0,.55); }
.band-dark h1{ margin:0 0 .35rem; font-weight:800; letter-spacing:.2px; color:#fff; }
.band-dark p{ margin:0; opacity:.97; color:#fff; }

/* alignment helpers */
.band-left   { justify-items: start;  text-align: left;  }
.band-center { justify-items: center; text-align: center; }
.band-right  { justify-items: end;    text-align: right; }

@media (max-width: 900px){
  .band-dark{ --band-height: 260px; }
}

</style>
